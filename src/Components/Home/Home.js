import React from 'react';
import List from '../Character/List';

function Home(props) {
    return (
        <div className= "container">
            <List/>
        </div>
    );
}

export default Home;