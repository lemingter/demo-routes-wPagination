import React from 'react';
import List from '../CharacterUI/List';

function Home(props) {
    return (
        <div className= "container">
            <List/>
        </div>
    );
}

export default Home;